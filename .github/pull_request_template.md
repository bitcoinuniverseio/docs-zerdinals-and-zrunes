## What this changes

<!-- Which pages, and why. Link the issue if one exists. -->

## Verified locally

- [ ] `npm test` passes (copy guard, public-safety scan, status data, Markdown lint)
- [ ] `npm run build` passes (includes internal link and anchor validation)
- [ ] I viewed the changed pages in the local preview
- [ ] Availability claims defer to the status page rather than restating it
- [ ] Any new or changed image has alt text and a MANIFEST.md entry
